<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout android:id="@+id/privacy_policy_dialog"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/transparent"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="30dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginVertical="@dimen/profile_view_marginVertical"
        android:background="@drawable/plantinfo_bg"
        >

        <LinearLayout
            android:orientation="vertical"
            android:layout_gravity="center"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginHorizontal="15dp"
            android:gravity="left"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <include
                android:id="@+id/login_dialog_all_check"
                layout="@layout/item_login_dialog"
                />

            <View
                android:layout_marginVertical="5dp"
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/black"
                android:alpha="0.2"
                />
            <include
                android:id="@+id/login_dialog_privacy"
                layout="@layout/item_login_dialog"
                />

            <include
                android:id="@+id/login_dialog_use"
                layout="@layout/item_login_dialog"
                />


            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/login_dialog_next_btn"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="30dp"
                android:layout_marginTop="30dp"
                style="@style/TextAppearance.AppCompat.Button"
                android:background="@drawable/bg_black_btn"
                android:textColor="@color/white"
                android:text="다음"
                android:fontFamily="@font/notosanskr_regular"
                android:textSize="14dp"
                android:minHeight="0dp"
                />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>


</androidx.constraintlayout.widget.ConstraintLayout>